class Product {
  String? imageURL;
  String? title;
  String? description;
  int? price;
  String? currency;

  Product(
      {this.imageURL, this.title, this.description, this.price, this.currency});
}
